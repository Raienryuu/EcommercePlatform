import { OrderStatusTransformPipe } from './order-status-transform.pipe';

describe('OrderStatusTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderStatusTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
